import 'package:flutter/material.dart';

class PollNumbers extends StatelessWidget {
  const PollNumbers({super.key});


  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Text("Poll numbers");
  }
}